collections:
  sections:
    output: false
  topics:
    output: true

defaults:
  - scope:
      path: "_sections/proposals"
    values:
      topic: "proposal"
  - scope:
      path: "_sections/development"
    values:
      topic: "development"
  - scope:
      path: "_sections/opportunities"
    values:
      topic: "opportunities"
  - scope:
      path: "_sections/slack"
    values:
      topic: "slack"
